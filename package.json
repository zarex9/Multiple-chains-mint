{
  "name": "multi-chain-nft",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:alfajores": "npx hardhat run scripts/deploy.js --network alfajores",
    "deploy:arbitrumGoerli": "npx hardhat run scripts/deploy.js --network arbitrumGoerli",
    "deploy:baseTest": "npx hardhat run scripts/deploy.js --network baseTest"
  },
  "devDependencies": {
    "hardhat": "^2.18.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.1.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.8.0",
    "@openzeppelin/contracts": "^4.9.3"
  }
}
